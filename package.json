{
  "name": "textlinker-web",
  "version": "1.0.0",
  "private": true,
  "description": "TextLinker web receiver and Android transfer companion.",
  "scripts": {
    "build": "vite build",
    "start": "node server.js"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
